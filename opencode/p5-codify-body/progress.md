# Progress — Project 5: Codify the Body

## Goal
Turn Project 4's orchestration into ONE re-runnable script.
Prove it is NOT a loop (no memory between runs).

## What This Proves
- One command runs the whole body
- Each candidate gets its own isolated checkout
- Reviewer grades each one (PASS/FAIL)
- Run it twice → it remembers nothing (no spine)

## Attempts

<!-- Each run appends here -->
<!-- NOTE: This file is NOT used by fix-all.sh -->
<!-- That's the point — this is an ENGINE, not a LOOP -->
